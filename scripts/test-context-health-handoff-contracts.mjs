import assert from 'node:assert/strict'
import { createServer } from 'vite'

const server = await createServer({ server: { middlewareMode: true }, appType: 'custom' })
const at = '2026-07-20T10:45:00.000Z'
const policyRef = `policies/context-health/sha256-${'1'.repeat(64)}`
const inputRef = `evidence/context-health-inputs/sha256-${'2'.repeat(64)}`
const decisionRef = `evidence/context-health-decisions/sha256-${'3'.repeat(64)}`
const manifestRef = `evidence/context-handoffs/sha256-${'4'.repeat(64)}`
const source = { kind: 'github', url: 'https://github.com/whatrune/sd-prompt-studio/issues/156' }
const provenance = { provenance_id: 'provenance-1', source_ref: source, source_kind: 'github', observed_at: at, verification_state: 'verified', immutable_revision: { kind: 'github_database_id', github_database_id: 156 } }
const bound = value => ({ state: 'bound', observed_at: at, value, provenance })
const identity = () => ({ canonical_task_record: source, dispatch_record: source, issue_binding: bound('https://github.com/whatrune/sd-prompt-studio/issues/156'), pr_binding: { state: 'not_created', observed_at: at, creation_record_ref: source }, branch_binding: bound('codex/implement-context-health-handoff-contracts'), worktree_binding: bound('.worktrees/context-health-handoff-contracts'), head_binding: bound('a'.repeat(40)), execution_binding: { state: 'not_applicable', observed_at: at, basis_ref: source, non_applicability_reason: 'contract_not_required' } })
const policy = (x = {}) => ({ contract_version: 'context-health-policy-v1', context_health_policy_ref: policyRef, policy_revision: 'v1', lifecycle_status: 'approved', evaluator_contract_version: 'context-health-evaluator-v1', checkpoint_rules: [{ rule_ref: 'rules/context-health/checkpoint/pre-review', checkpoint_type: 'pre_review', blocking_class: 'blocking', required_coverage_classes: [], protected_action_required: true }], atomic_signal_rules: [{ rule_ref: 'rules/context-health/atomic-signal/compression', atomic_signal_code: 'compression_event', authority: 'advisory', permitted_evidence_kinds: [], derived_signal_code: 'compression_pressure_observed' }], derived_signal_rules: [{ rule_ref: 'rules/context-health/derived-signal/compression', derived_signal_code: 'compression_pressure_observed', signal_class: 'soft', source_kind: 'atomic_signal', threshold: { kind: 'none' }, weight: 3 }], outcome_thresholds: { continue_min: 0, continue_max: 2, checkpoint_only_min: 3, checkpoint_only_max: 5, handoff_required_min: 6 }, consecutive_checkpoint_escalation: { rule_ref: 'rules/context-health/escalation/checkpoint-only', state: 'enabled', source_outcome: 'checkpoint_only', consecutive_count_threshold: 2, escalation_outcome: 'handoff_required', reset_outcomes: ['continue', 'handoff_required', 'hard_stop_and_handoff'] }, unknown_handling_rules: [{ rule_ref: 'rules/context-health/unknown-handling/advisory', evidence_class: 'advisory_signal', handling: 'report_without_score' }], override_rule: { rule_ref: 'rules/context-health/override/default', allowed_actor_roles: ['product_owner'], allowed_minimum_outcomes: ['checkpoint_only'], deescalation_allowed: false }, coverage_rules: [{ rule_ref: 'rules/context-health/coverage/task', coverage_class: 'task_assignment', applicable_checkpoint_types: ['pre_review'], required: true }], approved_by_roles: ['architect_team'], approval_record_ref: source, created_at: at, ...x })
const input = (x = {}) => ({ contract_version: 'context-health-evaluation-input-v1', context_health_input_ref: inputRef, policy_ref: policyRef, task_id: 'IMPLEMENT-CONTEXT-HEALTH-HANDOFF-CONTRACTS-001', assignment_revision: 1, role: 'backend_implementer', repository: 'whatrune/sd-prompt-studio', workflow_phase: 'implementation', checkpoint: { checkpoint_instance_id: 'checkpoint-1', checkpoint_type: 'pre_review', blocking_class: 'blocking', protected_action_ref: source }, workflow_identity: identity(), constraint_snapshot: { allowed_change_refs: [source], forbidden_change_refs: [source], required_validation_refs: [source], security_policy_refs: [source], approval_gate_refs: [source] }, counter_snapshot: { interaction_count: 0, operation_count: 0, unresolved_item_count: 0, active_blocker_count: 0, amendment_count: 0, settled_fact_correction_count: 0, dependency_count: 0, repeated_record_fetch_counts: [], settled_fact_reexplanation_counts: [] }, atomic_signal_observations: [{ observation_id: 'observation-1', atomic_signal_code: 'compression_event', presence: 'absent', authority: 'advisory', evidence_refs: [], observed_at: at }], canonical_record_coverage: [], validation_bindings: [], handoff_artifact: { state: 'none', observed_at: at, basis_ref: source }, prior_checkpoint: { state: 'none', first_checkpoint_basis_ref: source }, operator_override: { state: 'absent' }, evaluation_timestamp: at, ...x })
const decision = (x = {}) => ({ contract_version: 'context-health-decision-v1', context_health_decision_ref: decisionRef, context_health_input_ref: inputRef, context_health_policy_ref: policyRef, task_id: 'IMPLEMENT-CONTEXT-HEALTH-HANDOFF-CONTRACTS-001', assignment_revision: 1, role: 'backend_implementer', repository: 'whatrune/sd-prompt-studio', workflow_phase: 'implementation', checkpoint_instance_id: 'checkpoint-1', checkpoint_type: 'pre_review', outcome: 'continue', legal_action_class: 'assigned_work_allowed', required_artifact_class: 'decision_record', checkpoint_rule_ref: 'rules/context-health/checkpoint/pre-review', applied_atomic_signal_rule_refs: [], applied_coverage_rule_refs: [], hard_rule_refs: [], forced_handoff_rule_refs: [], soft_contributions: [], applied_unknown_handling_rule_refs: [], checkpoint_escalation_evaluation: { state: 'not_applicable', rule_ref: 'rules/context-health/escalation/checkpoint-only', reason_code: 'not-needed', pre_escalation_outcome: 'continue' }, operator_override_evaluation: { state: 'absent' }, unknown_advisory_signal_codes: [], prior_decision_refs: [], soft_score: 0, evaluator_contract_version: 'context-health-evaluator-v1', evaluation_timestamp: at, ...x })
const kinds = ['compressed_context_handoff', 'bootstrap_prompt', 'canonical_record_manifest', 'repository_state_snapshot', 'validation_snapshot']
const manifest = (x = {}) => ({ contract_version: 'context-handoff-manifest-v1', context_handoff_manifest_ref: manifestRef, task_id: 'IMPLEMENT-CONTEXT-HEALTH-HANDOFF-CONTRACTS-001', assignment_revision: 1, role: 'backend_implementer', repository: 'whatrune/sd-prompt-studio', workflow_phase: 'implementation', checkpoint_instance_id: 'checkpoint-1', context_health_decision_ref: decisionRef, canonical_task_record: source, dispatch_record: source, workflow_identity_snapshot: identity(), completed_work: [], frozen_decisions: [], unresolved_items: [], blockers_and_risks: [], forbidden_operations: [], validation_snapshot: { component_id: 'validation', component_kind: 'validation_snapshot', sha256: 'a'.repeat(64) }, exact_next_action: { action_id: 'review', owner_role: 'backend_architect', action_summary: 'review contracts', preconditions: [], source_refs: [source] }, component_artifacts: kinds.map((component_kind, i) => ({ component_id: `component-${i}`, component_kind, content_type: 'application/json', byte_length: 1, sha256: `${i}`.repeat(64) })), material_facts: [], freshness_evidence: [], redaction_records: [], source_provenance: [provenance], generator_contract_version: 'context-handoff-generator-v1', generated_at: at, ...x })

try {
  const api = await server.ssrLoadModule('/src/context-health/index.ts')
  const { validateContextHealthEvaluationInputV1: vi, validateContextHealthPolicyV1: vp, validateContextHealthDecisionV1: vd, validateContextHandoffManifestV1: vm, validateContextHandoffComponentValidationInputV1: vc, validateContextHandoffComponentValidationResultV1: vr, validateContextResumeValidationResultV1: vv, validateContextHealthDecisionSemantics, generateContextHealthPolicyRef, generateContextHealthInputRef, generateContextHealthDecisionRef, generateContextHandoffManifestRef, verifyContextHandoffManifestRef, verifyContextHealthReference, projectContextHandoffManifestV1 } = api
  assert.equal(vi(input(), at).accepted, true); assert(Object.isFrozen(vi(input(), at).value)); assert(Object.isFrozen(vi(input(), at).value.workflow_identity))
  assert.equal(vi(input({ workflow_identity: { ...identity(), issue_binding: { ...bound('i'), extra: true } } }), at).accepted, false)
  assert.equal(vi(input({ workflow_identity: { ...identity(), issue_binding: { state: 'not_created', observed_at: at, creation_record_ref: source } } }), at).accepted, false)
  assert.equal(vi(input({ operator_override: { state: 'bound', override_ref: source, actor_role: 'product_owner', minimum_outcome: 'handoff_required', reason_code: 'not-a-catalog-value', issued_at: at } }), at).accepted, false)
  assert.equal(vi(input({ workflow_phase: 'made-up-phase' }), at).accepted, false)
  assert.equal(vp(policy(), at).accepted, true); assert.equal(vp(policy({ checkpoint_rules: [{ ...policy().checkpoint_rules[0], unknown: true }] }), at).accepted, false)
  const softRule = policy().derived_signal_rules[0]
  const hardRule = { ...softRule, rule_ref: 'rules/context-health/derived-signal/security', derived_signal_code: 'security_boundary_uncertain', signal_class: 'hard_stop' }; delete hardRule.weight
  const forcedRule = { ...softRule, rule_ref: 'rules/context-health/derived-signal/handoff', derived_signal_code: 'operator_handoff', signal_class: 'forced_handoff' }; delete forcedRule.weight
  assert.equal(vp(policy({ derived_signal_rules: [hardRule] }), at).accepted, true, 'hard_stop without weight must be admitted')
  assert.equal(vp(policy({ derived_signal_rules: [forcedRule] }), at).accepted, true, 'forced_handoff without weight must be admitted')
  const { weight: ignoredSoftWeight, ...softWithoutWeight } = softRule
  assert.equal(vp(policy({ derived_signal_rules: [softWithoutWeight] }), at).accepted, false, 'soft without weight must be rejected')
  assert.equal(vp(policy({ derived_signal_rules: [{ ...softRule, weight: 0 }] }), at).accepted, false, 'soft with zero weight must be rejected')
  assert.equal(vp(policy({ derived_signal_rules: [{ ...softRule, weight: 1.5 }] }), at).accepted, false, 'soft with non-integer weight must be rejected')
  assert.equal(vp(policy({ derived_signal_rules: [{ ...hardRule, weight: 1 }] }), at).accepted, false, 'hard_stop with weight must be rejected')
  assert.equal(vp(policy({ derived_signal_rules: [{ ...forcedRule, weight: 1 }] }), at).accepted, false, 'forced_handoff with weight must be rejected')
  assert.equal(vd(decision(), at).accepted, true); assert.equal(vm(manifest(), at).accepted, true)
  assert.equal(vm(manifest({ component_artifacts: manifest().component_artifacts.slice(0, 4) }), at).accepted, false)
  assert.equal(validateContextHealthDecisionSemantics(decision({ applied_atomic_signal_rule_refs: ['rules/context-health/checkpoint/pre-review'] }), policy()).valid, false)
  assert.equal(vc({ contract_version: 'context-handoff-component-validation-input-v1', context_handoff_manifest: manifest(), component_payloads: kinds.map(component_kind => ({ component_kind, content_type: 'application/json', content_base64: 'e30=' })), validator_contract_version: 'context-handoff-component-validator-v1', validation_timestamp: at }, at).accepted, true)
  const componentResult = { contract_version: 'context-handoff-component-validation-result-v1', result_kind: 'valid', result_reason_code: 'all_components_valid', context_handoff_manifest_ref: manifestRef, component_results: kinds.map(component_kind => ({ component_kind, status: 'valid', computed_sha256: 'a'.repeat(64), computed_byte_length: 2, contract_version_state: 'matched', observed_contract_version: 'v1', diagnostic_ids: [] })), diagnostics: [], validator_contract_version: 'context-handoff-component-validator-v1', validated_at: at }
  assert.equal(vr(componentResult, at).accepted, true)
  assert.equal(vr({ ...componentResult, result_reason_code: 'component_validation_failed' }, at).accepted, false)
  assert.equal(vv({ contract_version: 'context-resume-validation-result-v1', result_kind: 'invalid', code: 'invalid_format', path: '$', message: 'bad', observed_at: at }, at).accepted, true)
  assert.equal(vv({ contract_version: 'context-resume-validation-result-v1', result_kind: 'invalid', code: 'invalid_format', path: '$', message: 'bad', observed_at: at, task_id: 'forbidden' }, at).accepted, false)
  const resumeCommon = { contract_version: 'context-resume-validation-result-v1', context_handoff_manifest_ref: manifestRef, workflow_identity_snapshot: identity(), task_id: 'IMPLEMENT-CONTEXT-HEALTH-HANDOFF-CONTRACTS-001', assignment_revision: 1, role: 'backend_implementer', repository: 'whatrune/sd-prompt-studio', resume_attempt_id: 'resume-1', source_verification_records: [], drift_records: [], unresolved_item_ids: [], diagnostics: [], gate_decision: { state: 'allowed', rule_refs: [], reason_code: 'fresh' }, legal_next_action: { action_class: 'continue', owner_role: 'backend_implementer', action_ref: source }, observed_at: at }
  assert.equal(vv({ ...resumeCommon, result_kind: 'success', drift_class: 'none', fresh_context_health_input_ref: inputRef, fresh_context_health_decision_ref: decisionRef, resume_validation_ref: inputRef }, at).accepted, true)
  assert.equal(vv({ ...resumeCommon, result_kind: 'blocked', blocked_reason_code: 'fresh_state_unavailable', decision_owner: 'backend_architect', recommended_next_action_code: 'fresh_fetch_and_retry', retry_policy: 'after_input_correction' }, at).accepted, true)
  assert.equal(vv({ ...resumeCommon, result_kind: 'stale', drift_class: 'review_required', stale_reason_codes: ['repository_head_changed'], amendment_required: true, decision_owner: 'backend_architect', recommended_next_action_code: 'request_architecture_amendment', retry_policy: 'after_architect_decision' }, at).accepted, true)
  assert.equal(vv({ ...resumeCommon, result_kind: 'internal_failure', internal_failure_code: 'resume_internal_failure', decision_owner: 'backend_implementer', recommended_next_action_code: 'implementation_review', retry_policy: 'after_implementation_fix' }, at).accepted, true)
  assert.equal(vv({ ...resumeCommon, result_kind: 'success', drift_class: 'none', fresh_context_health_input_ref: inputRef, fresh_context_health_decision_ref: decisionRef }, at).accepted, false)
  const p = policy(); p.context_health_policy_ref = await generateContextHealthPolicyRef(p); assert.equal(await verifyContextHealthReference(p), true)
  const hardPolicy = policy({ derived_signal_rules: [hardRule] }); hardPolicy.context_health_policy_ref = await generateContextHealthPolicyRef(hardPolicy); assert.equal(await verifyContextHealthReference(hardPolicy), true)
  const forcedPolicy = policy({ derived_signal_rules: [forcedRule] }); forcedPolicy.context_health_policy_ref = await generateContextHealthPolicyRef(forcedPolicy); assert.equal(await verifyContextHealthReference(forcedPolicy), true)
  assert.notEqual(await generateContextHealthPolicyRef(policy({ derived_signal_rules: [{ ...softRule, weight: 4 }] })), await generateContextHealthPolicyRef(policy()), 'soft weight remains part of the normative projection')
  const i = input(); i.context_health_input_ref = await generateContextHealthInputRef(i); assert.equal(await verifyContextHealthReference(i), true)
  const d = decision(); d.context_health_decision_ref = await generateContextHealthDecisionRef(d); assert.equal(await verifyContextHealthReference(d), true)
  const h = manifest(); h.context_handoff_manifest_ref = await generateContextHandoffManifestRef(h); assert.equal(await verifyContextHealthReference(h), true)
  for (const [root, field, generate] of [[p, 'context_health_policy_ref', generateContextHealthPolicyRef], [i, 'context_health_input_ref', generateContextHealthInputRef], [d, 'context_health_decision_ref', generateContextHealthDecisionRef]]) {
    const original = await generate(root); const changed = { ...root, [field]: 'invalid-self-reference', policy_revision: `${root.policy_revision ?? 'x'}-changed` }; assert.notEqual(await generate(changed), original)
    const reordered = { ...root, [field]: 'other' }; if (Array.isArray(root.approved_by_roles)) reordered.approved_by_roles = [...root.approved_by_roles].reverse(); if (Array.isArray(root.source_provenance)) reordered.source_provenance = [...root.source_provenance].reverse(); assert.equal(await generate(reordered), original)
  }
  const manifestProjection = projectContextHandoffManifestV1(h); const manifestComputed = await generateContextHandoffManifestRef(h)
  assert.equal(Object.keys(manifestProjection).length, 25); assert.equal(Object.hasOwn(manifestProjection, 'context_handoff_manifest_ref'), false); assert.match(manifestComputed, /^evidence\/context-handoffs\/sha256-[0-9a-f]{64}$/); assert.equal(await generateContextHandoffManifestRef(structuredClone(h)), manifestComputed); assert.equal((await verifyContextHandoffManifestRef(h, at)).result_kind, 'matched'); assert.equal(await verifyContextHealthReference(h), true)
  for (const changed of [{ ...h, context_handoff_manifest_ref: 'invalid-self-reference' }, { ...h, policy_revision: 'x-changed' }, { ...h, unknown_root: true }, { ...h, context_handoff_manifest_ref: 'invalid-self-reference', policy_revision: 'x-changed', unknown_root: true }]) assert.equal(await generateContextHandoffManifestRef(changed), manifestComputed)
  const storedTamper = { ...h, context_handoff_manifest_ref: 'invalid-self-reference' }; assert.equal((await verifyContextHandoffManifestRef(storedTamper, at)).result_kind, 'mismatch'); assert.equal(vm({ ...h, unknown_root: true }, at).accepted, false)
  const scalarTamper = { ...h, repository: 'whatrune/other' }; const nestedTamper = { ...h, workflow_identity_snapshot: { ...h.workflow_identity_snapshot, branch_binding: bound('other-branch') } }
  assert.notEqual(await generateContextHandoffManifestRef(scalarTamper), manifestComputed); assert.notEqual(await generateContextHandoffManifestRef(nestedTamper), manifestComputed); assert.equal((await verifyContextHandoffManifestRef({ ...scalarTamper, context_handoff_manifest_ref: manifestComputed }, at)).result_kind, 'mismatch')
  assert.equal(await generateContextHandoffManifestRef({ ...h, component_artifacts: [...h.component_artifacts].reverse() }), manifestComputed); const orderedA = { ...h, exact_next_action: { ...h.exact_next_action, preconditions: [source, { kind: 'github', url: 'https://github.com/whatrune/sd-prompt-studio/issues/157' }] } }; const orderedB = { ...orderedA, exact_next_action: { ...orderedA.exact_next_action, preconditions: [...orderedA.exact_next_action.preconditions].reverse() } }; assert.notEqual(await generateContextHandoffManifestRef(orderedA), await generateContextHandoffManifestRef(orderedB))
  const listA = { ...h, completed_work: [{ fact_id: 'work-1', fact_kind: 'completed_work', resolution_state: 'resolved', evidence_state: 'verified', owner_role: 'backend_implementer', value: { kind: 'string', string_value: 'a' }, source_refs: [source], freshness_evidence_ids: [], redaction_record_ids: [], provenance_ids: ['provenance-1'] }, { fact_id: 'work-2', fact_kind: 'completed_work', resolution_state: 'resolved', evidence_state: 'verified', owner_role: 'backend_implementer', value: { kind: 'string', string_value: 'b' }, source_refs: [source], freshness_evidence_ids: [], redaction_record_ids: [], provenance_ids: ['provenance-1'] }] }
  const listB = { ...listA, completed_work: [...listA.completed_work].reverse() }; assert.equal(await generateContextHandoffManifestRef(listA), await generateContextHandoffManifestRef(listB))
  const memberTamper = { ...listA, completed_work: [{ ...listA.completed_work[0], value: { kind: 'string', string_value: 'changed' } }, listA.completed_work[1]] }; assert.notEqual(await generateContextHandoffManifestRef(memberTamper), await generateContextHandoffManifestRef(listA))
  const factBase = { fact_id: 'fact-1', fact_kind: 'validation_result', resolution_state: 'resolved', evidence_state: 'verified', owner_role: 'backend_implementer', value: { kind: 'string', string_value: 'verified' }, source_refs: [source], freshness_evidence_ids: [], redaction_record_ids: [], provenance_ids: ['provenance-1'] }
  for (const value of [{ kind: 'string', string_value: 's' }, { kind: 'integer', integer_value: 1 }, { kind: 'boolean', boolean_value: true }, { kind: 'string_set', string_values: ['a', 'b'] }, { kind: 'reference', reference_value: source }]) assert.equal(vm(manifest({ material_facts: [{ ...factBase, value }] }), at).accepted, true)
  const { value: ignoredValue, ...notApplicableFact } = factBase
  assert.equal(vm(manifest({ material_facts: [{ ...notApplicableFact, resolution_state: 'not_applicable' }] }), at).accepted, true)
  assert.equal(vm(manifest({ material_facts: [{ ...factBase, resolution_state: 'not_applicable' }] }), at).accepted, false)
  assert.equal(vm(manifest({ material_facts: [{ ...factBase, evidence_state: 'stale' }] }), at).accepted, false)
  assert.equal(vm(manifest({ material_facts: [{ ...factBase, resolution_state: 'unresolved', evidence_state: 'unverified', provenance_ids: [] }] }), at).accepted, true)
  assert.equal(vm(manifest({ material_facts: [{ ...factBase, resolution_state: 'blocked', evidence_state: 'stale', freshness_evidence_ids: ['freshness-1'] }] }), at).accepted, true)
  assert.equal(vm(manifest({ material_facts: [{ ...factBase, value: { kind: 'unknown', text: 'no' } }] }), at).accepted, false)
  console.log('Context Health and Handoff contract tests passed.')
} finally { await server.close() }
